#############################
# CCycles wrapper
#############################

set(INC
  ..
)
set(INC_SYS
)

set(LIB
  cycles_device
  cycles_kernel
  cycles_scene
  cycles_session
  cycles_bvh
  cycles_subd
  cycles_graph
  cycles_util
)

#file( GLOB SRCS *.h *.cpp)
#add_library( ccycles STATIC ${SRCS} )

add_definitions(-DCCL_CAPI_DLL)

#set_target_properties(ccycles PROPERTIES LINKER_LANGUAGE CXX)
#target_include_directories(ccycles PRIVATE ../cycles/src)
#target_include_directories(ccycles PRIVATE ../../../../../../big_libs/OpenImageIO-2.2.19/include)
#target_include_directories(ccycles PRIVATE ../../../../../../big_libs/OpenEXR-2.5.5/include)

list(APPEND LIB ${OPENIMAGEIO_LIBRARIES})

if(WITH_ALEMBIC)
  add_definitions(-DWITH_ALEMBIC)
  list(APPEND INC_SYS
    ${ALEMBIC_INCLUDE_DIRS}
  )
  list(APPEND LIB
    ${ALEMBIC_LIBRARIES}
  )
endif()

if(WITH_CYCLES_OSL)
  list(APPEND LIB cycles_kernel_osl)
endif()

if(CYCLES_STANDALONE_REPOSITORY)
  list(APPEND LIB extern_sky)
else()
  list(APPEND LIB bf_intern_sky)
endif()

if(WITH_CYCLES_STANDALONE AND WITH_CYCLES_STANDALONE_GUI)
  list(APPEND INC_SYS
    ${Epoxy_INCLUDE_DIRS}
    ${SDL2_INCLUDE_DIRS}
  )
  list(APPEND LIB ${Epoxy_LIBRARIES} ${SDL2_LIBRARIES})
endif()

if(WITH_USD)
  # Silence warning from USD headers using deprecated TBB header.
  add_definitions(
    -D__TBB_show_deprecation_message_atomic_H
    -D__TBB_show_deprecation_message_task_H
  )

  list(APPEND INC_SYS
    ${USD_INCLUDE_DIRS}
  )
  list(APPEND LIB
    cycles_hydra
    ${USD_LIBRARIES}
  )
endif()

cycles_external_libraries_append(LIB)

# Common configuration.

include_directories(${INC})
include_directories(SYSTEM ${INC_SYS})

# Application build targets

if(WITH_CYCLES_STANDALONE)
  file( GLOB SRC *.cpp *.h *.c)

  # Windows resources: VERSIONINFO and the side-by-side private assembly
  # manifest are compiled into ccycles.dll at link time. Previously both were
  # bolted on afterwards by versioninfo_changer.ps1 driving ResourceHacker,
  # which meant an undocumented external tool and a manual step everyone had to
  # know about. See ccycles_manifest.xml.in for what the manifest is for.
  if(WIN32)
    # Members of the ccycles private assembly: every DLL that Rhino also ships
    # its own copy of, plus ccycles itself. Keep in sync with what actually
    # lands next to ccycles.dll in the install tree.
    set(CCYCLES_SXS_FILES
      ccycles.dll
      openimageio.dll
      openimageio_util.dll
      openvdb.dll
      tbb.dll
    )

    if(WITH_CYCLES_DEVICE_ONEAPI)
      list(APPEND CCYCLES_SXS_FILES cycles_kernel_oneapi_jit.dll sycl6.dll)
    endif()

    # OpenColorIO's DLL carries its soversion in the filename, so resolve the
    # real name rather than hardcoding opencolorio_2_2.dll as before.
    if(OPENCOLORIO_FOUND AND DEFINED OPENCOLORIO_LIBRARIES)
      foreach(_ocio_lib ${OPENCOLORIO_LIBRARIES})
        get_filename_component(_ocio_name "${_ocio_lib}" NAME_WE)
        if(_ocio_name)
          list(APPEND CCYCLES_SXS_FILES "${_ocio_name}.dll")
        endif()
      endforeach()
      unset(_ocio_name)
    endif()
    list(REMOVE_DUPLICATES CCYCLES_SXS_FILES)

    set(CCYCLES_ASSEMBLY_VERSION "1.0.0.0" CACHE STRING
      "assemblyIdentity version stamped into the ccycles SxS manifest")

    set(CCYCLES_SXS_FILE_ELEMENTS "")
    foreach(_sxs_file ${CCYCLES_SXS_FILES})
      string(APPEND CCYCLES_SXS_FILE_ELEMENTS "  <file name=\"${_sxs_file}\"/>\n")
    endforeach()
    unset(_sxs_file)

    configure_file(
      ${CMAKE_CURRENT_SOURCE_DIR}/ccycles_manifest.xml.in
      ${CMAKE_CURRENT_BINARY_DIR}/ccycles.manifest
      @ONLY
    )

    # A generated .rc that pulls the manifest in as resource type 24 under
    # ISOLATIONAWARE_MANIFEST_RESOURCE_ID (2), which is what the loader reads
    # for a DLL. The path is absolute so the RC compiler needs no extra -I.
    file(TO_NATIVE_PATH "${CMAKE_CURRENT_BINARY_DIR}/ccycles.manifest" _ccycles_manifest_path)
    string(REPLACE "\\" "\\\\" _ccycles_manifest_path "${_ccycles_manifest_path}")
    file(WRITE ${CMAKE_CURRENT_BINARY_DIR}/ccycles_manifest.rc
      "// Generated by CMake - do not edit.\n2 24 \"${_ccycles_manifest_path}\"\n")
    unset(_ccycles_manifest_path)

    list(APPEND SRC
      ${CMAKE_CURRENT_SOURCE_DIR}/ccycles_version.rc
      ${CMAKE_CURRENT_BINARY_DIR}/ccycles_manifest.rc
    )
  endif()

  add_library(ccycles SHARED ${SRC} ${INC} ${INC_SYS})
  unset(SRC)

  if(WIN32)
    # Stop the linker emitting its own manifest as resource 2, which would
    # collide with the one embedded above.
    set_property(TARGET ccycles APPEND_STRING PROPERTY LINK_FLAGS " /MANIFEST:NO")
  endif()

  target_link_libraries(ccycles PRIVATE ${LIB})

  if(APPLE)
    if(WITH_CYCLES_STANDALONE_GUI)
      set_property(TARGET ccycles APPEND_STRING PROPERTY LINK_FLAGS
        " -framework Cocoa -framework CoreAudio -framework AudioUnit -framework AudioToolbox -framework ForceFeedback -framework CoreVideo")
    endif()
  endif()

  if(UNIX AND NOT APPLE)
    set_target_properties(ccycles PROPERTIES INSTALL_RPATH $ORIGIN/lib)
  endif()

  if(CYCLES_STANDALONE_REPOSITORY)
    cycles_install_libraries(ccycles)
  endif()

  if(WITH_USD AND USD_LIBRARY_DIR)
    install(DIRECTORY
      ${USD_LIBRARY_DIR}/usd
      DESTINATION ${CMAKE_INSTALL_PREFIX}
    )

    install(DIRECTORY
      ${USD_LIBRARY_DIR}/../plugin/usd
      DESTINATION ${CMAKE_INSTALL_PREFIX}
    )
  endif()

  install(PROGRAMS
    $<TARGET_FILE:ccycles>
    DESTINATION ${CMAKE_INSTALL_PREFIX})
endif()
